
logging:
  level:
    root: INFO

spring:

  r2dbc:
    url: r2dbc:h2:mem:///testdb
    username: sa
    password:

  flyway:
    enabled: true
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=MYSQL
    baseline-on-migrate: true
    user: sa
    password:

